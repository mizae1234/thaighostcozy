# Custom Workspace Rules

- **Do NOT run `git push` or deployment commands (like `./deploy.sh`) automatically.** Always wait for the user to explicitly tell you to push the code or deploy it to the server.
